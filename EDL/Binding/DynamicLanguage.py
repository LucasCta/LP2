import math

def f(a):
    return math.sin(a)/10
#nome = sin, propriedade = implementacao, tempo = ligacao

print(10/10)
#nome = 1, propriedade = valor, tempo = compilacao

a = [30,40,50]
#nome = a, propriedade = endereco, tempo = execucao
#nome = a, propriedade = tipo, tempo = execucao

for i in range(0,len(a)):
    print(f(a))
    #nome = print, propriedade = definicao, tempo = implementacao